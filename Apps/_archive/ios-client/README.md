# iOS Client

`ios-client` is the Devys iPhone client for connecting to `mac-server` over Tailscale.

Current baseline capabilities:

- configure server URL
- fetch `/health`
- consume live `/stream` NDJSON events
