# Scribble

Some *italic* and **bold** text with a [link](https://example.com).

- Item one
- Item two

```swift
import Foundation

struct User {
    let id: Int
}
```

Inline `code()` and a block quote:

> Block quote with `inline` code.
