# Tree-sitter Query Assets

This directory contains the bundled Tree-sitter query assets used by DevysSyntax.

Layout:

- one subdirectory per language id
- `highlights.scm`
- `locals.scm`
- `injections.scm`

The initial Swift files are scaffolded as the first migration asset set.
The real query implementation lands in later migration tickets.
